你是一位精通 Flutter 3.41.x、Dart 3.11、go_router 17.1.0、get_it 9.2.1 和 flutter_bloc 9.2.0 的专家。请基于以下具体要求和约束，开发一个跨平台移动应用：

1.  **技术栈与代码质量**：
    *   项目必须严格使用指定的技术栈版本：Flutter 3.41.x、Dart 3.11、go_router 17.1.0（用于导航）、get_it 9.2.1（用于依赖注入）、flutter_bloc 9.2.0（用于状态管理）。
    *   所有提交的 Dart 代码必须通过 `dart analyze` 静态分析，结果为零警告、零错误。
    *   所有公开的类、方法、函数和重要逻辑块都必须包含详细的英文或中文注释，解释其目的、参数、返回值及关键实现逻辑。

2.  **架构与项目结构**：
    *   采用 **Clean Architecture** 进行全局设计，明确划分表现层（Presentation）、领域层（Domain）、数据层（Data）。
    *   项目文件结构必须清晰体现上述分层，例如：
        *   `lib/presentation/`: 包含 UI、页面、Widgets、BloCs/Cubits。
        *   `lib/domain/`: 包含实体（Entities）、仓库接口（Repository Interfaces）、用例（Use Cases）。
        *   `lib/data/`: 包含数据源（Data Sources）、模型（Models）、仓库实现（Repository Implementations）。
    *   严格遵守依赖规则：内层（如Domain）不依赖外层（如Data、Presentation），依赖方向由外向内。

3.  **数据存储与安全**：
    *   使用 **MMKV 2.4.0** 作为本地键值（KV）存储解决方案，用于存储用户偏好设置、阅读进度等轻量数据。
    *   对于需要加密的本地敏感数据（如用户令牌、配置信息），必须实现基于 AES-256-GCM 算法的加密存储与解密读取机制，确保密钥的安全管理。

4.  **UI/UX 与适配要求**：
    *   应用需同时适配手机和平板设备。在均为手机设备的情况下，应用在不同品牌、型号、屏幕尺寸的手机上，其UI布局、字体大小、间距、图标等视觉元素的显示效果必须保持高度一致。
    *   使用 Flutter 的 `MediaQuery`、`LayoutBuilder` 及相对单位（如 `Flexible`、`Expanded`、百分比）进行响应式设计，确保核心阅读界面在不同屏幕尺寸下均有良好的可用性。

5.  **核心功能规范（小说阅读器）**：
    *   支持解析和渲染本地 **TXT** 文件，要求自动识别编码（如UTF-8、GBK），并实现分页、章节检测、书签、进度保存功能。
    *   支持解析和渲染本地 **EPUB** 文件，要求能够提取元数据、目录（TOC）、章节内容，并提供流畅的分页或滚动阅读体验。
    *   支持 **在线HTML内容解析**，要求能从给定的URL或HTML字符串中提取纯净的正文文本，过滤广告、导航等无关元素，并格式化为适合阅读的样式。
    *   阅读器需提供至少以下功能：字体大小/类型调整、背景主题切换（日间/夜间/护眼模式）、行间距/段间距调整、亮度调节、目录跳转。

**输出要求**：请直接提供满足以上所有细化要求的、可执行的开发方案或关键模块的代码实现，并确保代码结构符合Clean Architecture，且通过`dart analyze`。